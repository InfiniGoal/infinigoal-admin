export type ProductImageUI = {
  image_url: string;     // storage URL later
  is_primary: boolean;
  position: number;
  variant_id?: string;
};


export type ImageItem = {
  id: string;
  image_url: string;
  is_primary: boolean;
  position: number;
};

////////////////////
